{
  "name": "lms-strapi-backend",
  "version": "1.0.0",
  "description": "Learning Management System with Strapi Cloud",
  "private": true,
  "scripts": {
    "build": "cd strapi && npm run build",
    "develop": "cd strapi && npm run develop",
    "start": "cd strapi && npm run start",
    "strapi": "cd strapi && npm run develop",
    "setup": "npm install && cd strapi && npm install",
    "setup-strapi": "node scripts/setup-strapi.js"
  },
  "dependencies": {
    "@strapi/strapi": "^4.15.0",
    "@strapi/plugin-users-permissions": "^4.15.0",
    "@strapi/plugin-i18n": "^4.15.0",
    "pg": "^8.11.3"
  },
  "overrides": {
    "@swc/core": "1.3.0",
    "glob": "10.0.0",
    "rimraf": "5.0.0",
    "@koa/router": "12.0.0"
  },
  "engines": {
    "node": ">=18.0.0 <=20.x.x",
    "npm": ">=6.0.0"
  }
}
